name: Build (stub)

# Same rationale as php-checks-stub.yml: no-op stand-in so the org ruleset's
# required "build / build" check is satisfied on this repo's own PRs.

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'true'
